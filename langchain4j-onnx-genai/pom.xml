<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.langchain4j</groupId>
        <artifactId>langchain4j-parent</artifactId>
        <version>1.0.0-beta4-SNAPSHOT</version>
        <relativePath>../langchain4j-parent/pom.xml</relativePath>
    </parent>

    <artifactId>langchain4j-onnx-genai</artifactId>
    <name>LangChain4j :: ONNX Runtime GenAI</name>

    <properties>
        <onnxruntime-genai.version>0.7.1</onnxruntime-genai.version>
        <onnxruntime.version>1.21.0</onnxruntime.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- ONNX Runtime GenAI -->
        <dependency>
            <groupId>com.microsoft.onnxruntime.genai</groupId>
            <artifactId>onnxruntime-genai</artifactId>
            <version>${onnxruntime-genai.version}</version>
        </dependency>

        <!-- Base ONNX Runtime -->
        <dependency>
            <groupId>com.microsoft.onnxruntime</groupId>
            <artifactId>onnxruntime</artifactId>
            <version>${onnxruntime.version}</version>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>1.4.11</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.0.0-M5</version>
                <configuration>
                    <systemPropertyVariables>
                        <onnxruntime-genai.native.onnxruntime.path>${project.build.directory}/lib-native/libonnxruntime.so</onnxruntime-genai.native.onnxruntime.path>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <dependencies>
                    <!-- Required for untar task -->
                    <dependency>
                        <groupId>org.apache.ant</groupId>
                        <artifactId>ant-compress</artifactId>
                        <version>1.5</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>download-and-extract-native-libs</id>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <target>
                                <echo message="OS Name: ${os.name}, OS Arch: ${os.arch}"></echo>

                                <!-- Define variables for download URLs and destination -->
                                <property name="onnxruntime.native.url" value="https://github.com/microsoft/onnxruntime/releases/download/v${onnxruntime.version}/onnxruntime-linux-x64-${onnxruntime.version}.tgz"></property>
                                <!-- Using the CPU version for Linux x64 -->
                                <property name="native.libs.dir" value="${project.build.directory}/lib-native"></property>
                                <property name="download.dir" value="${project.build.directory}/download"></property>

                                <!-- Create necessary directories -->
                                <mkdir dir="${native.libs.dir}"></mkdir>
                                <mkdir dir="${download.dir}"></mkdir>

                                <!-- Download ONNX Runtime native libraries (adjust URL if needed) -->
                                <get dest="${download.dir}/onnxruntime-native.tar.gz" src="${onnxruntime.native.url}" usetimestamp="true" verbose="true"></get>

                                <!-- Extract ONNX Runtime native libraries and rename libonnxruntime.so.${onnxruntime.version} to libonnxruntime.so -->
                                <untar compression="gzip" dest="${native.libs.dir}" src="${download.dir}/onnxruntime-native.tar.gz">
                                    <patternset>
                                        <include name="onnxruntime-linux-x64-${onnxruntime.version}/lib/libonnxruntime.so.${onnxruntime.version}"></include>
                                    </patternset>
                                    <mapper from="onnxruntime-linux-x64-${onnxruntime.version}/lib/libonnxruntime.so.${onnxruntime.version}" to="libonnxruntime.so" type="glob"></mapper>
                                </untar>
                            </target>
                        </configuration>
                    </execution>
                    <execution>
                        <id>download-test-model</id>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <phase>process-test-resources</phase>
                        <configuration>
                            <target>
                                <!-- Create directories
                                <mkdir dir="${project.build.directory}/test-models/SmolLM_135M_int4"></mkdir>
                                 -->

                                <!-- Download model files
                                <get dest="${project.build.directory}/test-models/tiny-llama-onnx/model.onnx" src="https://huggingface.co/HuggingFaceTB/SmolLM2-360M-Instruct/resolve/main/onnx/model_q4f16.onnx" usetimestamp="true" verbose="true"></get>

                                <get dest="${project.build.directory}/test-models/tiny-llama-onnx/genai_config.json" src="https://huggingface.co/HuggingFaceTB/SmolLM2-360M-Instruct/resolve/main/config.json" usetimestamp="true" verbose="true"></get>

                                <get dest="${project.build.directory}/test-models/tiny-llama-onnx/tokenizer/tokenizer.json" src="https://huggingface.co/HuggingFaceTB/SmolLM2-360M-Instruct/resolve/main/tokenizer.json" usetimestamp="true" verbose="true"></get>

                                <get dest="${project.build.directory}/test-models/tiny-llama-onnx/tokenizer/tokenizer_config.json" src="https://huggingface.co/HuggingFaceTB/SmolLM2-360M-Instruct/resolve/main/tokenizer_config.json" usetimestamp="true" verbose="true"></get>
                                 -->
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.0.0-M5</version>
                <configuration>
                    <systemPropertyVariables>
                        <onnxruntime-genai.native.onnxruntime.path>${project.build.directory}/lib-native/libonnxruntime.so</onnxruntime-genai.native.onnxruntime.path>
                    </systemPropertyVariables>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
