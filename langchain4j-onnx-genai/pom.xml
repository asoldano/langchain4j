<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.langchain4j</groupId>
        <artifactId>langchain4j-parent</artifactId>
        <version>1.0.0-beta4-SNAPSHOT</version>
        <relativePath>../langchain4j-parent/pom.xml</relativePath>
    </parent>

    <artifactId>langchain4j-onnx-genai</artifactId>
    <name>LangChain4j :: ONNX Runtime GenAI</name>

    <properties>
        <onnxruntime-genai.version>0.7.1</onnxruntime-genai.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- ONNX Runtime GenAI -->
        <dependency>
            <groupId>com.microsoft.onnxruntime.genai</groupId>
            <artifactId>onnxruntime-genai</artifactId>
            <version>${onnxruntime-genai.version}</version>
        </dependency>

        <!-- Base ONNX Runtime -->
        <dependency>
            <groupId>com.microsoft.onnxruntime</groupId>
            <artifactId>onnxruntime</artifactId>
            <version>1.20.0</version>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>1.4.11</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Download test model from Hugging Face -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>download-test-model</id>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <phase>process-test-resources</phase>
                        <configuration>
                            <target>
                                <!-- Create directories
                                <mkdir dir="${project.build.directory}/test-models/SmolLM_135M_int4"></mkdir>
                                 -->

                                <!-- Download model files
                                <get dest="${project.build.directory}/test-models/tiny-llama-onnx/model.onnx" src="https://huggingface.co/HuggingFaceTB/SmolLM2-360M-Instruct/resolve/main/onnx/model_q4f16.onnx" usetimestamp="true" verbose="true"></get>

                                <get dest="${project.build.directory}/test-models/tiny-llama-onnx/genai_config.json" src="https://huggingface.co/HuggingFaceTB/SmolLM2-360M-Instruct/resolve/main/config.json" usetimestamp="true" verbose="true"></get>

                                <get dest="${project.build.directory}/test-models/tiny-llama-onnx/tokenizer/tokenizer.json" src="https://huggingface.co/HuggingFaceTB/SmolLM2-360M-Instruct/resolve/main/tokenizer.json" usetimestamp="true" verbose="true"></get>

                                <get dest="${project.build.directory}/test-models/tiny-llama-onnx/tokenizer/tokenizer_config.json" src="https://huggingface.co/HuggingFaceTB/SmolLM2-360M-Instruct/resolve/main/tokenizer_config.json" usetimestamp="true" verbose="true"></get>
                                 -->
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
